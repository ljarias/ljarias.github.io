name: Publicar IA al Día

on:
  push:
    branches: ["main"]
  schedule:
    - cron: "15 7 * * *"
      timezone: "America/Bogota"
  workflow_dispatch:
    inputs:
      regenerar_infografia:
        description: "Regenerar la infografía de hoy aunque ya exista"
        required: false
        type: boolean
        default: false

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Descargar repositorio
        uses: actions/checkout@v6

      - name: Verificar configuración del agente
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          TODAY=$(TZ=America/Bogota date +'%Y-%m-%d')
          EVENT="${{ github.event_name }}"
          NEED_AI=false

          if [[ "$EVENT" == "schedule" || "$EVENT" == "workflow_dispatch" ]]; then
            NEED_AI=true
          elif [[ "$EVENT" == "push" && -f "_posts/${TODAY}-ia-al-dia.md" && ! -f "assets/infografias/${TODAY}.png" ]]; then
            # Permite crear la infografía inicial de una edición ya publicada
            # cuando el código del generador acaba de incorporarse.
            NEED_AI=true
          fi

          echo "AI_NEEDED=$NEED_AI" >> "$GITHUB_ENV"

          if [[ -z "$OPENAI_API_KEY" ]]; then
            echo "::warning::Falta el secreto OPENAI_API_KEY. Se publicará el sitio sin generar contenido automático."
            echo "AI_ENABLED=false" >> "$GITHUB_ENV"
          else
            echo "AI_ENABLED=true" >> "$GITHUB_ENV"
          fi

      - name: Configurar Python
        if: env.AI_ENABLED == 'true' && env.AI_NEEDED == 'true'
        uses: actions/setup-python@v7
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Instalar dependencias del agente
        if: env.AI_ENABLED == 'true' && env.AI_NEEDED == 'true'
        run: pip install -r requirements.txt

      - name: Generar resumen diario
        id: generar
        continue-on-error: true
        if: (github.event_name == 'schedule' || github.event_name == 'workflow_dispatch') && env.AI_ENABLED == 'true'
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_MODEL: gpt-5.6-luna
        run: python scripts/generar_resumen.py

      - name: Avisar si falló el resumen
        if: steps.generar.outcome == 'failure'
        run: |
          echo "::warning::El agente no pudo generar la edición de hoy. El sitio se publicará con el contenido existente."

      - name: Generar infografía diaria
        id: infografia
        continue-on-error: true
        if: env.AI_ENABLED == 'true' && env.AI_NEEDED == 'true' && (github.event_name == 'push' || steps.generar.outcome == 'success')
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          INFOGRAPHIC_MODEL: gpt-image-2.5-flare-2026-09-08
          FORCE_INFOGRAFIA: ${{ inputs.regenerar_infografia }}
        run: python scripts/generar_infografia.py

      - name: Avisar si falló la infografía
        if: steps.infografia.outcome == 'failure'
        run: |
          echo "::warning::No se pudo crear la infografía de hoy. La edición textual seguirá publicándose normalmente."

      - name: Guardar edición e infografía en GitHub
        if: env.AI_ENABLED == 'true' && env.AI_NEEDED == 'true'
        run: |
          if [[ -n "$(git status --porcelain _posts data _data assets/infografias 2>/dev/null)" ]]; then
            git config user.name "ia-al-dia-bot"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add _posts data _data assets/infografias 2>/dev/null || true
            git commit -m "Publicar IA al Día $(TZ=America/Bogota date +'%Y-%m-%d')"
            git push
          else
            echo "No hay cambios editoriales ni visuales para guardar."
          fi

      - name: Configurar GitHub Pages
        uses: actions/configure-pages@v5

      - name: Construir sitio Jekyll
        uses: actions/jekyll-build-pages@v1
        with:
          source: ./
          destination: ./_site

      - name: Preparar artefacto de Pages
        uses: actions/upload-pages-artifact@v4
        with:
          path: ./_site

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    permissions:
      pages: write
      id-token: write
    steps:
      - name: Publicar en GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
